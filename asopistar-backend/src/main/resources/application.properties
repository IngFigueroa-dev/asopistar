# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/asopistar
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=negocio
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
# Clave secreta: mínimo 32 caracteres para HMAC-SHA256
jwt.secret=asopistar_clave_secreta_super_larga_y_segura_2025
# Expiración en milisegundos: 86400000 = 24 horas

# Server
server.port=8080
server.servlet.context-path=/api


# ── Correo Gmail ─────────────────────────────────────────────────
# PASOS PARA CONFIGURAR:
# 1. cuenta Gmail → Seguridad → Verificación en dos pasos 
# 2. Seguridad → Contraseñas de aplicaciones
# 3. Seleccionar "Correo" y "Otro" → escribe "ASOPISTAR"
# 4. Copiar la contraseña de 16 caracteres generada y pégarla abajo
# 5. NUNCA subir este archivo a Git (está en .gitignore)
spring.mail.username=adminasopistar@gmail.com
spring.mail.password=nrmb qykc fakb ckvj

# ── URL del frontend ─────────────────────────────────────────────
app.url.frontend=http://localhost:5173